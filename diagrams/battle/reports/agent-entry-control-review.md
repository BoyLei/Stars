# Scope

Reviewed battle documentation for entry, auto battle, and control-layer claims:

- `diagrams/battle/L0_ENTRY_LAYER.md`
- `diagrams/battle/FLOW_AUTO_BATTLE_DETAILED.md`
- `diagrams/battle/L2_CONTROL_LAYER.md`
- `diagrams/battle/_Nav_Battle.md` entries related to L0, L2, and auto battle

Code evidence was gathered first through CodeGraph for the target modules, then minimal line-range reads were used where CodeGraph elided large method bodies.

# Follow-up 2026-07-13

- `FLOW_AUTO_BATTLE_DETAILED.md` 追加复核：`BattleManager.UseSkill()` 不直接发送技能协议或进入客户端预播；它只把技能槽映射成按键并调用 `InputManager.DispatchVKey(vkey, 2, true)`。后续由 `UniversalButton.InputVKey(arg == 2)` 模拟 `OnPointerDown(null)` + `OnPointerUp(null)`，再经 `SkillComponent.SendUserSkillReq()` 进入 `SkillController.ClientUseSkill(...)`。
- 代码证据：`BattleManager.UseSkill()` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2524-2544`; `UniversalButton.InputVKey` at `Assets/Scripts/StarGame/UI/SkillBtn/UniversalButton.cs:906-956`; `SkillComponent.SendUserSkillReq` / `SkillController.ClientUseSkill` call at `Assets/Scripts/StarGame/Game/Player/Component/SkillComponent.cs:542-553,1188`。
- `FLOW_AUTO_BATTLE_DETAILED.md` 三次复核：`AutoBattleUseSkill()` at `BattleManager.cs:2180-2209` 先 `MarkDirtyState(false)`、调用 `TryUseSkill()`，随后无论 `TryUseSkill()` 返回值都执行 `skillContainer.MarkAutoBattleCD()`；`GotoFightEnemy()` at `BattleManager.cs:1986-2098` 仅在 `AutoBattleUseSkill(...)` 返回 true 后执行 `StartGlobalSkillCD()` 并清空 `ignoreSkillPoss`。
- `FLOW_AUTO_BATTLE_DETAILED.md` 三次复核：`InputManager.DispatchVKey()` at `InputManager.cs:946-1027` 最终触发 `OnVirtualInput?.Invoke(vkey,arg)`；`UniversalButton.Awake()` at `UniversalButton.cs:195-205` 订阅 `OnVirtualInput += InputVKey`，`UniversalButton.Reset()` at `UniversalButton.cs:318-328` 取消订阅。
- 本报告下方 `Required Fixes` 是当时发现的问题清单；当前根文档已同步：`FLOW_AUTO_BATTLE_DETAILED.md` 修正 `MarkAutoBattleCD()` / `StartGlobalSkillCD()` 顺序和虚拟按键链路，`L2_CONTROL_LAYER.md` 修正 CtrlGroup 组件阶段、RPC 方法清单和 Partner 技能按钮归属，`_Nav_Battle.md` 删除未确认入口并改为已补证入口。
- `L2_CONTROL_LAYER.md` 二次复核：`GameManager.CreateEntity(E_EntityType.BulletEntity)` 走 `CreateBulletSummon(gameCommand)`，而 `CreateBulletSummon()` 直接复用 `CreateSummon(gameCommand)` 创建 `SummonCtrlGroup`；当前策划语义的 BulletSummon 控制链不是直接 new `BulletEntityCtrl`。
- `L2_CONTROL_LAYER.md` 二次复核：`SummonCtrlGroup.OnBulletCreateRet()` 转发给 `m_CurrentCtrledVitBase.OnBulletCreateRet(...)`；`OnBulletEndRet()` 转发后用 `bulletEndRet.OwnerEntityID` 构造 `E_Command.Destroy`，帧末调用 `GameManager.EntityDataCommand(...)`。
- `L2_CONTROL_LAYER.md` 二次复核：`SummonCtrlGroup.CreateMContainer()` 对 `E_EntityType.BulletEntity` 不进入主角召唤物 `DotRemoveRoot` 分支；`BulletEntityCtrl` 仍存在，但属于程序定义 `BulletEntity` 支线。
- `L2_CONTROL_LAYER.md` 三次复核：`PlayerCtrlGroup` 也有 RPC 回调重写，原清单遗漏；当前已补 `OnBuffCreateRet/OnBuffEndRet/OnRuntimeSyncRet/OnRunStageRet/OnRunStageForceEndRet/OnServerSetPosNTF/OnCDUpdateNotice/OnSkillUseRet/OnSkillEndRet`，证据为 `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:1078-1245`。
- `L2_CONTROL_LAYER.md` 三次复核：`SkillComponent.SendUserSkillReq()` 不是在 `ClientUseSkill` 后直接发 RPC；它先组 `SkillUseReq/BlackList`，在 `Assets/Scripts/StarGame/Game/Player/Component/SkillComponent.cs:1188` 调 `SkillController.ClientUseSkill(...)`。`SkillControllerSkillPartial.ClientUseSkill(SkillContainer, SkillUseReq, ..., float)` at `Assets/Scripts/StarGame/Game/Skill/SkillPartial/SkillControllerSkillPartial.cs:745-751` 转入 `UseSkill(...)`，后者在 `596-727` 按 `CheckCanUseSkill` 结果选择 `SendPreUseSkillReq` / `UseNewSkill` / `CacheUserInput` / `PrePlayUserInput` 等路径。
- 2026-07-15 五次复核：`L0_ENTRY_LAYER.md` 已把 L1 工厂初始化时点拆清：`GameManager.Init()` at `Assets/Scripts/StarGame/Game/GameManager.cs:235-255` 初始化 `SimpleDataFactory/DynamicDataFactory/EntityFactory/ViewFactory`；`CreateGame()` at `GameManager.cs:1799-1838` 只保障 `EntityFactory.Init()` 与 `ViewFactory.Init(...)`。
- 2026-07-15 六次复核：`FLOW_AUTO_BATTLE_DETAILED.md` 的自动战斗主链再次按代码图核实：`AutoBattleUseSkill()` 先 `TryUseSkill()` 后无条件 `MarkAutoBattleCD()`；`GotoFightEnemy()` 只在成功分支调用 `StartGlobalSkillCD()`；`UseSkill()` 只调用 `InputManager.DispatchVKey(vkey, 2, true)`，由 `UniversalButton.InputVKey(arg == 2)` 模拟按钮按下/抬起后回到 `SkillComponent.SendUserSkillReq()`。
- 2026-07-15 七次复核：`L2_CONTROL_LAYER.md` 已把 BulletSummon 回包拆成两段：`SummonCtrlGroup.OnBulletCreateRet()` 通过 `SummonEntityBase.OnBulletCreateRet()` 转到 `SkillControllerBulletPartial.OnBulletCreateRet()` 并创建 `SkillBullet`；`SummonCtrlGroup.OnBulletEndRet()` 先通过 `SummonEntityBase.OnBulletEndRet()` 转到 `SkillControllerBulletPartial.OnBulletEndRet()` 释放 `SkillBullet`，再用 `bulletEndRet.OwnerEntityID` 组 `E_Command.Destroy` 并在帧末调用 `GameManager.EntityDataCommand(...)` 销毁 BulletSummon 控制实体。
- 2026-07-15 八次复核：`L0_ENTRY_LAYER.md` 已把 `DynamicRenderQueueManager` 收紧为代码事实：`AddNewRender()` 调 `DealDynamicMetrals()`，按 `MaterialRenderQueueSTAND` / Shader 名设置 `material.renderQueue` 与 `enableInstancing`；`Init()` 检测 `supportsEarlyZ` 并设置主相机 depth texture；描边线材质按 `SceneScore < MAT_OVER_SCORE` 保留。`GraphicsSettings.useScriptableRenderPipelineBatching` 是 `AppMain` 画质设置中的独立开关，不再写成该管理器的直接调用链。
- 2026-07-15 九次复核：`L0_ENTRY_LAYER.md` 已把 `GameManager.InputVKey -> PlayerCtrlGroup.InputVKey` 收窄为移动虚拟键链路；代码图确认 `PlayerCtrlGroup.InputVKey()` 只调用 `DoVKey_Move()`，后者只处理 `GameVKey.MoveX_CMD/MoveZ_CMD`，手动技能按钮入口仍以 L3/FLOW 技能文档为准。
- 2026-07-15 十次复核：`FLOW_AUTO_BATTLE_DETAILED.md` 自动战斗链再次按代码图确认：`GotoFightEnemy()` 只有在 `AutoBattleUseSkill(...)` 返回 true 时调用 `StartGlobalSkillCD()`；`AutoBattleUseSkill()` 内部先 `TryUseSkill()`，随后无条件 `skillContainer.MarkAutoBattleCD()`；`UseSkill()` 只调用 `InputManager.Instance.DispatchVKey(vkey, 2, true)`，后续由 `UniversalButton.InputVKey(arg == 2)` 模拟按钮按下/抬起并进入 `SkillComponent.SendUserSkillReq()`。按钮后的效果链同步收紧为手动 `SkillEntityActionPartial` 与 ServerControl/Bullet 等 `StageHandle` 分叉。本地 `mmdc` 不存在，现有 `FLOW_AUTO_BATTLE_DETAILED.svg` 是 2026-07-10 旧渲染文件，根文档已标为历史 SVG，当前事实以 Mermaid 源为准。
- 2026-07-16 十一次复核：`FLOW_AUTO_BATTLE_DETAILED.md` 已拆清 `AutoBattleBtn.OnClick` 与 `OnEnable` 缓存恢复：点击走 `BattleManager.SwitchAutoBattle()`，缓存恢复读取 `AUTOBATTLE_OPEN` 后直接 `Start()` / `Stop()`。`L0_ENTRY_LAYER.md` 已修正 `EnterFrame(frameIndex)` 的帧号逻辑为 `frameIndex < 0` 时递增、否则赋值，并补充 `StarWorldGame.OnEnterFrame()` 并列调用 `GameManager.EnterFrame()` / `BattleManager.EnterFrame()` / `ClientNpcManager.EnterFrame()`。`L2_CONTROL_LAYER.md` 已把 `GameContext` 创建点从 `CreateGame()` 收紧到 `CreateMap(GameParam)`。

# Evidence Table

| md file | claim checked | verdict | code evidence |
|---|---|---|---|
| `L0_ENTRY_LAYER.md:25-28` | `GameManager.EnterFrame(frameIndex)` drives frame state, entity controllers, local entities, map, and timeout handling. | 正确 | `GameManager.EnterFrame` updates `m_context.currentFrameIndex` at `Assets/Scripts/StarGame/Game/GameManager.cs:2772-2787`, loops `m_listEntityCtrl[i].EnterFrame` at `2795-2801`, loops `m_LocalEntitys[j].EnterFrame` at `2803-2806`, calls `m_map.EnterFrame` at `2841-2844`, and `HandleOverTimeMsgRet()` at `2846-2847`. |
| `L0_ENTRY_LAYER.md:30-33` | `GameInput -> GameManager.InputVKey -> PlayerCtrlGroup.InputVKey -> DoVKey_Move` 仅描述移动虚拟键链路。 | 已修正 | `StarWorldGame.Start` registers `InputManager.Instance.OnVirtualInput += OnVKey` at `Assets/Scripts/StarGame/Module/StarWorldModule/StarWorldGame.cs:98-100`; `StarWorldGame.OnVKey` calls `GameManager.Instance.InputVKey(vkey, arg, mainPlayerId)` at `294-298`; `GameManager.InputVKey` casts `GetEntityCtr(playerId)` to `PlayerCtrlGroup` and calls `player.InputVKey(vkey,arg)` at `Assets/Scripts/StarGame/Game/GameManager.cs:2455-2471`；代码图确认 `PlayerCtrlGroup.InputVKey()` at `PlayerCtrlGroup.cs:730-735` 只调用 `DoVKey_Move()`，`DoVKey_Move()` at `:700-728` 只处理 `MoveX_CMD/MoveZ_CMD`。 |
| `L0_ENTRY_LAYER.md:57` | `EnterFrame(int)` / `EnterFixLaterFrame()` are frame-driven entries. | 正确 | `StarWorldGame.FixedUpdate` calls `OnEnterFrame(FrameIndex)` at `Assets/Scripts/StarGame/Module/StarWorldModule/StarWorldGame.cs:304-318`; `OnEnterFrame` calls `GameManager.Instance.EnterFrame(frameId)` and `BattleManager.Instance.EnterFrame(frameId)` at `334-338`; `LaterFixUpdate` calls `GameManager.Instance.EnterFixLaterFrame()` at `325-329`. |
| `L0_ENTRY_LAYER.md:80` | L1 工厂初始化来自 `GameManager.Init()`，`CreateGame()` 只保障 Entity/View 两个模式内入口。 | 已修正 | `GameManager.Init()` calls `SimpleDataFactory.Init()`, `DynamicDataFactory.Init()`, `EntityFactory.Init()`, and `ViewFactory.Init(...)` at `Assets/Scripts/StarGame/Game/GameManager.cs:235-255`; `CreateGame()` only calls `EntityFactory.Init()` and `ViewFactory.Init(...)` at `1799-1838`, while the `DynamicDataFactory.Init()` line there is commented. |
| `L0_ENTRY_LAYER.md:64-66` | `GameInput.Create/AddSkillBtn/SetTouchState`; joystick goes to `InputManager.OnJoystickMove(dir)`. | 正确 | `GameInput.Create` loads `UIDef.MobileController` and sets `Instance` at `Assets/Scripts/StarGame/Game/GameInput.cs:105-140`; `AddSkillBtn` stores and reparents button at `177-183`; `OnEnable` subscribes `m_AnalogStick.BtnDirChange += On_JoystickMove` at `548-559`; `On_JoystickMove` calls `InputManager.Instance.OnJoystickMove(direction)` at `593-596`; `SetTouchState` begins at `598-606`. |
| `L0_ENTRY_LAYER.md:74,76-77` | `GameManager` is `ServiceModule`, `GameContext` is data, moving VKey chain reaches `PlayerCtrlGroup.DoVKey_Move`. | 已修正 | CodeGraph output: `GameManager -> ServiceModule`, `GameContext` source at `Assets/Scripts/StarGame/Game/GameContext.cs:11-57` contains fields/methods and no singleton base; `GameManager.InputVKey` / `PlayerCtrlGroup.InputVKey` evidence above. |
| `L0_ENTRY_LAYER.md:68-75` | `DynamicRenderQueueManager` renderQueue / EarlyZ / 描边阈值描述 | 已修正 | `DynamicRenderQueueManager.AddNewRender()` at `Assets/Scripts/StarGame/Game/DynamicRenderQueueManager.cs:302-306` 调 `DealDynamicMetrals()`；后者按 `GetRenderQueueFromShaderName()` 设置 `material.renderQueue` / `material.enableInstancing`，并对 `SGAME/Roles/CartoonChar_4_Line` 用 `SceneScore < MAT_OVER_SCORE` 决定是否保留描边线材质；`Init()` at `DynamicRenderQueueManager.cs:72-111` 检测 `supportsEarlyZ` 并设置 `MainCamera.depthTextureMode`；`AppMain.UseHighHighHighLevelSetting/UseHighLevelSetting/UseMiddleLevelSetting/UseLowLevelSetting/UseLowestLevelSetting` 写 `MAT_OVER_SCORE` 为 80/50/0 等值。 |
| `FLOW_AUTO_BATTLE_DETAILED.md:14-28` | Core auto-battle chain from button/cache to `SwitchAutoBattle`, `Start`, fixed update, search, move, and skill use. | 正确 | `AutoBattleBtn.OnClick` calls `BattleManager.Instance.SwitchAutoBattle()` at `Assets/Scripts/StarGame/Game/Skill/AutoBattle/AutoBattleBtn.cs:106-108` (CodeGraph); `AutoBattleBtn.OnEnable` restores cache and calls `Start/Stop` at `34-53` (CodeGraph); `SwitchAutoBattle` branches to `Start/Stop` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:1407-1420`; `Start` registers `MonoHelper.AddFixedUpdateListener(OnFixedUpdate, Battle)` at `1512-1538`; `OnFixedUpdate -> OnTick -> SearchEnemyFight` at `1337-1405`; `SearchEnemyFight -> SearchEnemy/GotoFightEnemy` at `1651-1661`; `GotoFightEnemy` uses `GetAutoBattleCanUseSkillContainer`, `CheckIsSkillArea`, `AutoBattleUseSkill`, `FindDistanceValidPoint`, and `TryFindPath(...AutoBattle)` at `1986-2089`; `TryUseSkill -> UseSkill` at `2257-2268`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:41-45,153-154` | `Start()` opens state, registers events/fixed update, breaks path/follow, refreshes skill cache, marks dirty. | 正确 | `BattleManager.Start` checks forbid, calls `UpdateBateState(Open,true)`, `OnEventListener`, `MonoHelper.AddFixedUpdateListener`, `RecordAutoBattleStartPos`, `PlayerCtrlGroup.BreakFindPath`, `BreakFollowDynamicEnity`, `RefreshCacheSkillPosContainers`, and `MarkDirtyState(true)` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:1512-1538`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:48-64,155-157` | `OnFixedUpdate()` handles global CD, coolTime, execute guards, dirty tick, thinking frequency, and partner checks. | 正确 | `BattleManager.IsExecuteAutoBattle` guards main player, open state, hold, finding path, and death at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:1305-1335`; `OnFixedUpdate` handles `globalSkillCD`, `coolTime`, `IsExecuteAutoBattle`, `dirtyAutoSkill -> OnTick`, `THINKING_FREQUENCY`, and partner `TriggerPartnerSkill` at `1337-1395`; `OnTick` calls `SearchEnemyFight()` at `1397-1405`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:81-86,137-138` | Successful auto skill flow is shown as `AutoBattleUseSkill -> TryUseSkill/UseSkill -> MarkAutoBattleCD -> StartGlobalSkillCD`. | 需修正 | Actual order is split across caller and callee: `AutoBattleUseSkill` calls `MarkDirtyState(false)`, `TryUseSkill`, and `skillContainer.MarkAutoBattleCD()` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2180-2208`; the caller only calls `StartGlobalSkillCD()` after `AutoBattleUseSkill(...)` returns true at `2042-2048`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:89-94,159` | If target is outside skill range, auto battle computes skill distance, finds a valid point, and calls `PlayerCtrlGroup.TryFindPath(...AutoBattle)`. | 正确 | `GotoFightEnemy` checks `CheckIsSkillArea` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2034-2039`; computes `SkillAutoUseDistance / 100f` at `2075-2076`; calls `FindPathManager.Instance.FindDistanceValidPoint` at `2085`; calls `(M_MainPlayerCtrlBase as PlayerCtrlGroup).TryFindPath(..., E_FindPathType.AutoBattle)` at `2088-2089`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:160-162` | `AutoBattleUseSkill` clears dirty state, calls `TryUseSkill`, and `TryUseSkill` calls `UseSkill`. | 正确 | `AutoBattleUseSkill` clears dirty state and calls `TryUseSkill(skillContainer)` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2180-2189`; `TryUseSkill` calls `UseSkill(skillContainer)` at `2257-2268`; `UseSkill` simulates skill-key input with `InputManager.Instance.DispatchVKey(...)` at `2524-2544`. |
| `FLOW_AUTO_BATTLE_DETAILED.md:165-169` | Auto battle ultimately reuses the same skill system as manual skill input and enters the skill pipeline. | 正确 | Auto battle reaches `UseSkill`, which dispatches the configured skill key through `InputManager.Instance.DispatchVKey` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2524-2544`; manual skill UI sends skill requests through `SkillComponent.SendUserSkillReq` and `SkillController.ClientUseSkill` at `Assets/Scripts/StarGame/Game/Player/Component/SkillComponent.cs:842,1188` (rg evidence). |
| `L2_CONTROL_LAYER.md:31-35` | `EntityCtrlBase` is the base for Player/NPC/Monster/Partner/Summon/Bullet style controllers; Player owns SkillComponent plus components. | 正确 | CodeGraph output: `PlayerCtrlGroup -> EntityCtrlBase`, `GameNPCCtrlGroup -> EntityCtrlBase`, `MonsterCtrlGroup -> EntityCtrlBase`, `PartnerCtrlGroup -> EntityCtrlBase`; `PlayerCtrlGroup` class at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:50`; `EntityCtrlBase` class at `Assets/Scripts/StarGame/Game/Player/EntityCtrlBase.cs:23`; `PlayerCtrlGroup.g_SkillComponent` at `PlayerCtrlGroup.cs:108` (rg evidence). |
| `L2_CONTROL_LAYER.md:41-45,96` | Three-stage async loading creates components in subclass callbacks, but exact examples say `OnTemplateCreateFinifh` creates `UnitPendant/CheckIndicator` and `OnActionOnViewCreateFinifh` creates `SkillIndicator`. | 需修正 | This is true for `PlayerCtrlGroup`: `OnTemplateCreateFinifh` adds `UnitPendant` and `CheckIndicator` at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:269-285`; `OnActionOnViewCreateFinifh` adds `SkillIndicator` for main player at `302-315`. It is not universal: `GameNPCCtrlGroup.OnTemplateCreateFinifh` adds `UnitPendant` at `GameNPCCtrlGroup.cs:156-163` and `OnActionOnViewCreateFinifh` adds `CheckIndicator` at `183-197` (CodeGraph), not the same stage split. |
| `L2_CONTROL_LAYER.md:47-50,91-92` | `SkillComponent` handles skill button input and sends skill requests. | 正确 | `SkillComponent.Init(EntityCtrlBase, EntityCtrlBase)` is defined at `Assets/Scripts/StarGame/Game/Player/Component/SkillComponent.cs:62-97` (CodeGraph); pointer handlers call `SendUserSkillReq` at `SkillComponent.cs:505,553,592,628` (rg evidence); `SendUserSkillReq` begins at `842` and calls `SkillController.ClientUseSkill` at `1188` (rg evidence). |
| `L2_CONTROL_LAYER.md:69-72,98` | `PlayerCtrlGroup` drives components and `g_SkillComponent` separately each frame. | 正确 | `PlayerCtrlGroup.EnterFrame` loops `m_listCompoent[i].EnterFrame(frameIndex)` and separately calls `g_SkillComponent?.EnterFrame(frameIndex)` at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:471-479`. |
| `L2_CONTROL_LAYER.md:83-85,98` | `PlayerCtrlGroup` exposes `g_SkillComponent`, and `UpdatePlayerSkill()` creates/refreshes it. | 正确 | `PlayerCtrlGroup.UpdatePlayerSkill` creates `new SkillComponent()`, calls `Init(this)`, and registers death callback at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:438-445`; refreshes existing component when `GameInput.Instance != null` at `447-454`; releases it at `528`. |
| `L2_CONTROL_LAYER.md:87-89` | CtrlGroup RPC method list. | 已修正 | Current CodeGraph evidence: `PlayerCtrlGroup` has `OnBuffCreateRet`, `OnBuffEndRet`, `OnRuntimeSyncRet`, `OnRunStageRet`, `OnRunStageForceEndRet`, `OnServerSetPosNTF`, `OnCDUpdateNotice`, `OnSkillUseRet`, `OnSkillEndRet` at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:1078-1245`; `MonsterCtrlGroup` has `OnBuffCreateRet`, `OnBuffEndRet`, `OnRuntimeSyncRet`, `OnSkillUseRet`, `OnSkillEndRet`, `OnRunStageRet`, `OnRunStageForceEndRet`, but no `OnBulletCreateRet`; `PartnerCtrlGroup.OnCDUpdateNotice` does exist at `Assets/Scripts/StarGame/Game/Player/PartnerCtrlGroup.cs:432-439`, plus `OnRunStage*` and `OnServerSetPosNTF`; `GameNPCCtrlGroup.LookAtMainPlayer` and `SetVisiable` exist. |
| `L2_CONTROL_LAYER.md:99` | `PartnerCtrlGroup` aggregates partner skill slots. | 需修正 | `PartnerCtrlGroup.CreateSkillComponent` only invokes `GlobalEvent.OnPartnerCreate.Invoke(this)` for main-player summons at `Assets/Scripts/StarGame/Game/Player/PartnerCtrlGroup.cs:160-175`; comments say partner buttons are created in `PlayerCtrlGroup` and multiple partners refresh them at `162-169`. The direct aggregation owner is not `PartnerCtrlGroup` alone. |
| `_Nav_Battle.md:11,14,24` | Nav entries for L0, L2, and auto battle correctly describe the linked docs. | 正确 | `L0_ENTRY_LAYER.md` covers `GameManager/GameInput` at lines `56-67`; `L2_CONTROL_LAYER.md` covers control groups and component mode at lines `75-92`; `FLOW_AUTO_BATTLE_DETAILED.md` describes `AutoBattleBtn -> BattleManager -> FixedUpdate -> search -> move -> skill` at lines `13-28` and is supported by BattleManager evidence above. |
| `_Nav_Battle.md:57-58` | Known limitation says auto-battle decision module and L0 driver source are uncertain. | 需修正 | These are no longer uncertain for the reviewed scope: auto battle decision flow is in `BattleManager.OnFixedUpdate/OnTick/SearchEnemyFight/GotoFightEnemy` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:1337-1405,1651-1661,1986-2089`; L0 driver source is `StarWorldGame.FixedUpdate -> OnEnterFrame -> GameManager.EnterFrame/BattleManager.EnterFrame` at `Assets/Scripts/StarGame/Module/StarWorldModule/StarWorldGame.cs:304-340`. |

# Required Fixes

1. `FLOW_AUTO_BATTLE_DETAILED.md:81-86,137-138`
   - 原文定位：图和时序图把 `MarkAutoBattleCD()` / `StartGlobalSkillCD()` 都画在释放成功链路内。
   - 问题：`MarkAutoBattleCD()` 在 `AutoBattleUseSkill()` 内无条件执行；`StartGlobalSkillCD()` 是调用方在 `AutoBattleUseSkill(...) == true` 后执行。
   - 代码证据：`BattleManager.AutoBattleUseSkill` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:2180-2208`; caller success branch at `2042-2048`.
   - 建议替换文本：`AutoBattleUseSkill()` 先 `MarkDirtyState(false)`，调用 `TryUseSkill()`，随后对当前技能槽执行 `MarkAutoBattleCD()`；`GotoFightEnemy()` 仅在 `AutoBattleUseSkill()` 返回 true 后调用 `StartGlobalSkillCD()` 并清空 `ignoreSkillPoss`。

2. `L2_CONTROL_LAYER.md:41-45,96`
   - 原文定位：`OnTemplateCreateFinifh（壳子加载完）→ new UnitPendant/CheckIndicator`，`OnActionOnViewCreateFinifh（模型加载完）→ new SkillIndicator`。
   - 问题：这是 `PlayerCtrlGroup` 的具体行为，不是所有 CtrlGroup 的统一三段式组件位置。
   - 代码证据：`PlayerCtrlGroup.OnTemplateCreateFinifh` at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:269-285`; `PlayerCtrlGroup.OnActionOnViewCreateFinifh` at `302-315`; `GameNPCCtrlGroup.OnTemplateCreateFinifh` / `OnActionOnViewCreateFinifh` at `Assets/Scripts/StarGame/Game/Player/GameNPCCtrlGroup.cs:156-197` (CodeGraph).
   - 建议替换文本：`Create -> OnTemplateCreateFinifh -> OnActionOnViewCreateFinifh` 是控制组常见生命周期；组件由各子类按需注册。`PlayerCtrlGroup` 在壳子完成时添加 `UnitPendant/CheckIndicator`，模型完成时为主角添加 `SkillIndicator`；`GameNPCCtrlGroup` 等子类的组件注册位置不同。

3. `L2_CONTROL_LAYER.md:87-89`
   - 原文定位：`MonsterCtrlGroup.OnBulletCreateRet` 与 Partner/GameNPC 方法清单。
   - 问题：当前 `MonsterCtrlGroup.cs` 未找到 `OnBulletCreateRet`；旧 review 里认为 `PartnerCtrlGroup.OnCDUpdateNotice` 不存在的结论已过期，当前代码实际存在该方法。
   - 代码证据：CodeGraph output for `MonsterCtrlGroup.cs` lists `OnBuffCreateRet`, `OnBuffEndRet`, `OnRuntimeSyncRet`, `OnSkillUseRet`, `OnSkillEndRet`, `OnRunStageRet`, `OnRunStageForceEndRet`; no `OnBulletCreateRet`. `PartnerCtrlGroup.OnCDUpdateNotice` exists at `Assets/Scripts/StarGame/Game/Player/PartnerCtrlGroup.cs:432-439`, and Partner also has `OnRunStageRet/OnRunStageForceEndRet/OnServerSetPosNTF`.
   - 建议替换文本：`MonsterCtrlGroup.OnBuffCreateRet/OnBuffEndRet/OnRuntimeSyncRet/OnSkillUseRet/OnSkillEndRet/OnRunStageRet/OnRunStageForceEndRet`；`PartnerCtrlGroup.OnBuffCreateRet/OnBuffEndRet/OnCDUpdateNotice/OnRuntimeSyncRet/OnSkillUseRet/OnSkillEndRet/OnRunStageRet/OnRunStageForceEndRet/OnServerSetPosNTF`；`GameNPCCtrlGroup.LookAtMainPlayer/SetVisiable`。

4. `L2_CONTROL_LAYER.md:99`
   - 原文定位：`PartnerCtrlGroup 挂到主角根节点 + 聚合技能槽`。
   - 问题：代码证据显示 Partner 侧通过事件通知主角/外部刷新伙伴技能按钮，不能简单表述为 `PartnerCtrlGroup` 自己聚合技能槽。
   - 代码证据：`PartnerCtrlGroup.CreateSkillComponent` comments and `GlobalEvent.OnPartnerCreate.Invoke(this)` at `Assets/Scripts/StarGame/Game/Player/PartnerCtrlGroup.cs:160-175`; `RomveSkillComponent` starts at `179`; `PlayerCtrlGroup.UpdatePlayerSkill` owns `g_SkillComponent` creation at `Assets/Scripts/StarGame/Game/Player/PlayerCtrlGroup.cs:438-445`.
   - 建议替换文本：`PartnerCtrlGroup` 在主角召唤物创建/离开时通过 `GlobalEvent.OnPartnerCreate/OnLeaveSpace` 通知伙伴技能按钮刷新；`SkillComponent` 的实际创建仍由主角侧 `PlayerCtrlGroup.g_SkillComponent` 路径承担。

5. `_Nav_Battle.md:57-58`
   - 原文定位：`AutoBattle 决策逻辑所在模块`、`L0 驱动源未确认` 仍列为已知局限。
   - 问题：本次已确认这两项。
   - 代码证据：`BattleManager.OnFixedUpdate/OnTick/SearchEnemyFight/GotoFightEnemy` at `Assets/Scripts/StarGame/Service/BattleManager/BattleManager.cs:1337-1405,1651-1661,1986-2089`; `StarWorldGame.FixedUpdate/OnEnterFrame` at `Assets/Scripts/StarGame/Module/StarWorldModule/StarWorldGame.cs:304-340`.
   - 建议替换文本：删除这两项局限，或改为：`AutoBattle 决策主线已确认在 BattleManager；L0 帧驱动源已确认由 StarWorldGame.FixedUpdate/MonoHelper 注册链路触发。`

# Unverified Claims

- `DynamicRenderQueueManager` 已在 2026-07-15 八次复核补证；仍不把 SRP Batcher 写成该管理器的直接调用链。
