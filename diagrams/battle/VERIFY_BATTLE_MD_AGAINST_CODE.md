# Battle MD 代码核验报告

> 日期：2026-07-11
> 范围：`diagrams/battle/*.md` 根文档 + `diagrams/battle/reports/*.md` 报告文档。
> 方法：按模块派发 4 个子 agent 复核，并由主线程用 CodeGraph / 定向文件检查合并修正。旧 `reports/agent-*.md` 保留为历史草稿，当前事实以根文档和 `reports/*-review.md` 为准。

## 证据来源

| 模块 | 新复核报告 |
|---|---|
| 入口 / 自动战斗 / 控制层 | `reports/agent-entry-control-review.md` |
| 技能核心 / partial / 释放流程 | `reports/agent-skill-review.md` |
| 效果 / 伤害 / Buff / Bullet / Passive | `reports/agent-effect-review.md` |
| 实体工厂 / 运行时 / L5 / 计划 | `reports/agent-entity-support-plan-review.md` |
| Game 补充目录归属 | `reports/agent-game-extra-review.md` |

## 根文档状态

| 文档 | 状态 | 关键修正 |
|---|---|---|
| `_Nav_Battle.md` | 已修正 | 删除 AutoBattle/L0 驱动未确认说法；技能/伤害/Buff 摘要改为已验证链路；L5 摘要补为地图/ClientNpc/快照现状/相机/特效/音效；文件数口径改为“已覆盖 220”，ClientNpc/Data/Object/Partner 20 个补充文件已分别折入 L5/L1/L2；原始 reports 明确降级为历史输入，当前结论以 L*/FLOW/VERIFY/review 为准。 |
| `BATTLE_FRAMEWORK_ARCHITECTURE.md` | 已修正 | 手动技能入口改为 SkillComponent -> SkillController，并补足 `ClientUseSkill -> UseSkill -> UseNewSkill -> CreateSkillEntity -> SkillEntity.ClientUseSkill -> OnClientPreEnter -> EnterCurStage`；技能主轴补 `SkillContainer`；运行时 tick 改为 `NPCEntityBase.EnterFrame -> SkillDispatcher.EnterFrame`，不写成 SkillComponent 驱动技能状态 tick；手动效果入口补为 `SkillStage.ExecuteFrameEvents -> SkillEntityActionPartial.PlayStageEffect/TryPlayEffect`，`StageHandle` 限定为 ServerControl/Bullet 等路径；自动战斗补为 `BattleManager -> InputManager.DispatchVKey -> SkillComponent/SkillController`；`FuncOnTryPlayClientEffect` 改为先回 SkillController / NPCEntityBase，再按 Damage 分支进入 EffectUtils；PassiveInfo 改为配置，运行时为 PassiveSkillEntity；Buff/Bullet/Passive 协议入口补为 `GameManager.HandleRPCMsg -> EntityCtrlMsgBase/CtrlGroup`，运行时创建再由 `SkillController*Partial` 处理；伤害链拆为 `EffectUtils -> HandleHurtNodeMsg/ActionOnHurtNodeMsg -> BattleManager.OnHurtData -> DamageEntity/DamageViewNew`，总图时序显式补 `BattleManager` 参与者，不再合并成 `DamageEntity / HandleHurtNodeMsg`；补充 `SkillBullet` 与 `E_EntityType.BulletEntity -> SummonCtrlGroup` 的命名边界；SnapShot 拆清为 `SerSnapshotSeqManager` 历史注释化、`ServiceSnapshotData` 子类仍作动态缓存；相机支撑改为 CameraManager 存在、普通技能 Camera 链当前 `SkillController.OnActionPlayCamera()` 开头 `return`，CameraShake 仍走 `OnActionPlayCameraShake -> GlobalEvent.OnVirtualCameraShakeEvent`；补音效支撑 `ViewVitalNPCNormal -> SoundManager/Wwise`；L1 工厂节点改为 `L1 Factories`，不再用 `EntityFactory` 单名承载四个工厂；SVG 标为历史渲染版本。 |
| `BATTLE_OVERVIEW.md` | 已修正 | EntityFactory 不再写成单口三链分发；全景图改为 `GameManager -> EntityFactory/DynamicDataFactory/SimpleDataFactory/ViewFactory` 并列入口，Recycler 标注为各工厂各自持有；AOI/表现链改成代码证实的继承和引用关系；总览图移除 `DynamicDataFactory -> L1R`，改为 DynamicDataObject 数据对象边界；`GM -> SD` 改为实体运行时创建/驱动 SkillDispatcher；Passive 节点改为 `PassiveSkillEntity + PassiveInfo 配置`；Snapshot 改为 `SerSnapshotSeqManager` 历史化、`ServiceSnapshotData` 缓存仍用；TypeEffect 标为工厂映射 17 类；DamageEntity 改为经 `BattleManager.OnHurtData` 的飘字/UI实体；`AutoBattle` 改为 `AutoBattleBtn/SwitchEnemyBtn`；补充 Bullet 命名边界：`SkillBullet` 是 L4 运行时，`E_EntityType.BulletEntity` 当前实体创建链复用 `SummonCtrlGroup`；`CusQueue` 改为真实类名 `QueueExtends/CusListQueue`；文件数口径改为“已覆盖 220”，ClientNpc/Data/Object/Partner 20 个补充文件已折入现有 L1/L2/L5 层级。 |
| `FLOW_AUTO_BATTLE_DETAILED.md` | 已修正 | `MarkAutoBattleCD()` 与 `StartGlobalSkillCD()` 顺序改为代码实际顺序：`AutoBattleUseSkill()` 内部先 `TryUseSkill()`、随后无论结果都执行 `skillContainer.MarkAutoBattleCD()`，`GotoFightEnemy()` 仅在返回 true 后执行 `StartGlobalSkillCD()` 并清空 `ignoreSkillPoss`；自动战斗 `UseSkill()` 说明改为 `InputManager.DispatchVKey(vkey, 2, true)` -> `OnVirtualInput` -> `UniversalButton.InputVKey(arg == 2)` -> `SkillComponent.SendUserSkillReq()` 的虚拟按键复用链路，并补充 `UniversalButton.Awake/Reset` 订阅边界；按钮后效果链收紧为手动 `SkillEntityActionPartial` 与 ServerControl/Bullet 等 `StageHandle` 分叉；2026-07-16 复核补充 `AutoBattleBtn.OnClick` 才走 `SwitchAutoBattle()`，`OnEnable` 缓存恢复读取 `AUTOBATTLE_OPEN` 后直接 `Start()` / `Stop()`；SVG 标为历史渲染版本，当前事实以 Mermaid 源为准。 |
| `FLOW_BUFF_LIFECYCLE.md` | 已修正 | Buff 创建/结束/回收改为 `GameManager.HandleRPCMsg -> EntityCtrlMsgBase/CtrlGroup -> SkillControllerBuffPartial -> SkillBuff` 与后续帧 `ReleaseTempBuffs/ReleaseItemBuff` 延迟释放；首次 `OnFrameInit` 才播放 Buff 特效/循环特效并清除 `needExecuteFrameInit`，不写成每帧播放；BlackBoard 链路补为 `HandleBlackList/HandleItemBlackBoard/HandleBuffBlackBoardNode` 与 `SkillStage.OnRunStageRet` 分工；二次复核确认 `EnterFrameBuff` 上游是 `SkillDispatcher.EnterFrame -> SkillController.EnterFrame -> EnterFrameBuff`，且 `SkillBuff.Create(...)` 只做运行时初始化、`CreateStageHandle()`、`InitBuff(...)` 和 Buff 变化通知；三次复核把 `SkillControllerBuffPartial` 从“协议入口”收紧为 CtrlGroup 之后的 Buff 运行时处理器；四次复核补 `HandleBuffBlackBoardNode -> dirtyUpdateBuff -> ActionOnUpdateBuff/ActionOnBuffUpdate` 与 `EntityBaseData.TriggerBuffChange -> BuffEventData/TriggerChange` 的通知边界，不写成属性/伤害数值计算入口；六次复核把“存疑点”改成已验证边界：客户端 `buffDic` 按 `RuntimeID` 管理，`OnBuffCreateRet()` 只处理 `RuntimeID` 重复释放，不按 `BuffID` 本地合并；`StackCount` 由服务器黑板同步，`Replace/MaxFloor` 只保留为配置侧字段边界；Skill 目录定向代码图未发现 `SkillBuff.AddBuff()` / `SkillBuff.RemoveBuff()` / `PassiveInfo.TriggerPassive()` 精确方法名。 |
| `FLOW_DAMAGE_PIPELINE.md` | 已修正 | Bullet 创建改为 `OnBulletCreateRet`；二次复核补充 `SkillBullet.Create(...)` 的 `hasCreate` 防重复创建/AOI 通知逻辑，以及 `OnBulletEndRet -> SkillBullet.OnBulletEndRet -> ReleaseBullet` 的结束立即回收边界；阶段效果入口拆为手动 `SkillEntityActionPartial.PlayStageEffect/TryPlayEffect` 与 ServerControl/Bullet 等 `StageHandle.PlayStageEffect/TryPlayEffect` 两条；客户端效果委托改为 `FuncOnTryPlayClientEffect -> StageTryPlayClientEffect/TryPlayClientEffect -> NPCEntityBase.PlayClientSkillEffect`，并补充 `Damage` 分支先走 `HandleClientEffectDamage -> NPCEntityBase.HandleEffectDamage(...) -> EffectUtils.HandleEffectDamage(...)` 或 `HandleEffectDamageTargetArr -> EffectUtils.HandleEffectDamageTar(...)`，`DamageSecond` 分支走 `HandleClientDamageSecond -> EffectUtils.HandleEffectDamageSecond(...)`；伤害落地改为 `HandleEffectDamage -> AOIEntityObject.HandleHurtNodeMsg(delegate) -> NPCEntityBase.OnHurtNodeMsg -> BattleManager.OnHurtData -> PlayDamageText -> DamageEntity.Create -> DamageViewNew.BindDamageEntity`；补充 `HandleEffectDamageTar` 只播放客户端目标受击特效/动作、不进入飘字链，`HandleEffectDamageSecond` 只转交 `HandleHurtNodeMsg`；`TypeEffectFactory.Create` 调用方改为 `SkillBullet.PlayEffects` / `ServerControlStageEntityBase.PlayEffects`；DamageEntity 明确为飘字/UI实体，`Create()` 只保存 `DamageData/HurtData`、加载 `DamageViewNew` 并绑定 UI，不写成伤害计算入口；2026-07-16 复核同步 Bullet/Buff 方法枚举顺序为先 `CreateStageHandle()` 再创建配置 Info。 |
| `FLOW_SKILL_CAST_FULL.md` | 已修正 | 手动施法不走 `GameManager.InputVKey`；手动阶段效果改为 `SkillStage.ExecuteFrameEvents -> SkillEntityActionPartial.PlayStageEffect/TryPlayEffect`，`StageHandle` 限定为 ServerControl/Bullet 等路径；Buff/Bullet/Passive 协议入口补为 `GameManager.HandleRPCMsg -> EntityCtrlMsgBase/CtrlGroup`，运行时创建再由 `SkillController*Partial` 处理；自动战斗通过 `DispatchVKey(arg == 2)` 模拟按钮；SVG 标为历史渲染版本，当前事实以 Mermaid 源为准。 |
| `FLOW_SKILL_RELEASE.md` | 已修正 | 手动释放入口补为 `UniversalButton.OnPointerDown/OnPointerUp/OnEndDrag/onPressing -> SkillComponent.OnPointerDown/OnPointerUp/OnEndDrag/OnPressingAction -> SendUserSkillReq -> SkillController.ClientUseSkill -> UseSkill -> UseNewSkill -> CreateSkillEntity -> SkillEntity.ClientUseSkill -> OnClientPreEnter -> EnterCurStage`；每帧链路改为 `SkillEntity.OnUpdate -> SkillStage.OnUpdate/ExecuteFrameEvents -> SkillEntityActionPartial.PlayStageEffect/TryPlayEffect`，不再写成 `Tick(dt)` 或手动路径统一走 `StageHandle`。 |
| `L0_ENTRY_LAYER.md` | 已修正 | 入口、帧驱动、GameInput 链路由 entry-control 报告证明；五次复核把 L1 工厂初始化时点拆清：`GameManager.Init()` 初始化 `SimpleDataFactory/DynamicDataFactory/EntityFactory/ViewFactory`，`CreateGame()` 只保障 `EntityFactory/ViewFactory` 的模式内初始化；八次复核把 `DynamicRenderQueueManager` 收紧为 `AddNewRender -> DealDynamicMetrals -> material.renderQueue/enableInstancing`、`supportsEarlyZ` depth texture 和 `SceneScore < MAT_OVER_SCORE` 描边阈值，不再写成 SRP Batcher 直接调用链；九次复核把 `InputVKey` 收窄为 `GameManager.InputVKey -> PlayerCtrlGroup.InputVKey -> DoVKey_Move` 的 MoveX/MoveZ 移动虚拟键链，手动技能按钮入口仍以 L3/FLOW 文档为准；2026-07-16 复核补充 `GameManager.EnterFrame(frameIndex)` 中 `frameIndex < 0` 才递增 `currentFrameIndex`，否则赋值为入参，且 `StarWorldGame.OnEnterFrame()` 并列调用 `GameManager.EnterFrame()` / `BattleManager.EnterFrame()` / `ClientNpcManager.EnterFrame()`。 |
| `L1_ENTITY_FACTORY_LAYER.md` | 已修正 | API 改为 `InstanceEntity<T>` / `InstanceData<T>` / `CreateViewAsync` / `Recycler.Pop/Push/Release`；文件口径改为 Factory 13 + LocalDynamic 根目录 4 + Base 2 + Static 1；图形关系改为 `EntityFactory/DynamicDataFactory/SimpleDataFactory/ViewFactory` 并列入口，各自持有 Recycler，不再画成 `EntityFactory -> 其它工厂`；`EntityFactory` 池 key 改为 `type.FullName`，池化对象改为 `EntityObject`；`ReleaseEntity(EntityObject)` 收窄为标记释放，真正回池在 `ClearReleasedObjects()`；`DynamicDataFactory` 只写成 DynamicDataObject 数据对象入口；`CreateViewAsync` 改为推荐路径：callback 返回 GameObject 后 `SetView`；`CreateView/CreateViewAddressables` 标注为 `[Obsolete]` 旧同步接口；`Stone.cs` 标注为普通 MonoBehaviour 静态脚本。 |
| `L1_ENTITY_RUNTIME_LAYER.md` | 已修正 | 删除未证实 `OnAOIUpdate/SetVisible/ViewAOI.OnVisible`；改为属性回调和 View 引用链；View 更新策略不再泛化为每帧 Update/脏标记；三次复核把可见性主链从 `ViewVitalNPCNormal.OnActionVisible(bool)` 收紧为 `EntityRemoteDynamic.ControlShowHide(EntityShowHidenTag,bool) -> ViewVitalNPCNormal.OnLogicControlShow(...)`；八次复核补充 `ViewAOI.ControllShowHide -> M_EntityBase.ControlShowHide?.Invoke(Self,isShow) -> ViewVitalNPCNormal.OnLogicControlShow(...)`，且 `ViewVitalNPCNormal.OnEventListener/OffEventListener` 订阅/反订阅 `ControlShowHide`，代码图未发现 `OnActionVisible(bool)` 调用方；二次复核补充 `E_EntityType.BulletEntity` 当前创建链是 `GameManager.CreateBulletSummon -> CreateSummon -> SummonCtrlGroup`，而 `BulletEntity/BulletEntityCtrl` 是代码中存在的程序定义 Bullet 支线；`AttacState/AttackState` 只保留代码可证的 `VitalState` 继承与 `OnEnable` 差异；补充 Data 8 文件折入：`EntityBaseData.UpdateWithAttr` 由 `GameManager.UpdateEntityData/HandlePropSync/CreateEntityData` 调用，`HandleClientBattleStates` 由 GameManager 与 Skill 层调用。 |
| `L2_CONTROL_LAYER.md` | 已修正 | 组件注册阶段按 Player/GameNPC 差异收紧；Player 方法清单补 `OnBuffCreateRet/OnBuffEndRet/OnRuntimeSyncRet/OnRunStageRet/OnRunStageForceEndRet/OnServerSetPosNTF/OnCDUpdateNotice/OnSkillUseRet/OnSkillEndRet`；Monster 方法清单移除未找到的 `OnBulletCreateRet`；Partner 方法清单按当前代码补 `OnCDUpdateNotice/OnRunStage*/OnServerSetPosNTF`；伙伴技能按钮归属按事件通知 + PlayerCtrlGroup.g_SkillComponent 说明；`BulletCtrl` 改为真实类 `BulletEntityCtrl`（文件 `BulletCtrl.cs`），但现行 `E_EntityType.BulletEntity` 创建不直接 new 它，而是 `CreateBulletSummon -> CreateSummon -> SummonCtrlGroup`；七次复核把 BulletSummon 回包拆成两段：`SummonCtrlGroup -> SummonEntityBase -> SkillControllerBulletPartial -> SkillBullet` 的运行时创建/释放，以及 `SummonCtrlGroup.OnBulletEndRet` 帧末按 `bulletEndRet.OwnerEntityID` 销毁控制实体；L0 边界改为 `GameManager.Init/CreateGame/CreateMap` 初始化、`CreateEntity` 创建、`EnterFrame` 驱动、移动虚拟键 `InputVKey -> PlayerCtrlGroup.InputVKey -> DoVKey_Move`，其中 `CreateGame()` 初始化工厂/相机/运行状态，`CreateMap(GameParam)` 懒创建并重置 `GameContext`；`SendUserSkillReq` 改为组装 `SkillUseReq/BlackList` 后交给 `SkillController.ClientUseSkill`，发送/预播/缓存由 `SkillController.UseSkill` 分支决定；补充 Object 2 文件折入 `E_EntityType.Interact -> CreateInterActionObject -> ObjectCtrlGroup`，PartnerManager 1 文件折入 `AppMain.InitServices -> PartnerManager.Init` 和 `BattleManager.GetCanUsePartner -> GetInPlayedPartners`。 |
| `L3A_SKILL_ENGINE_CORE.md` | 已修正 | `SkillDispatcher` 不再写成输入接收者；`VKey` 移动链收紧为 `GameManager.InputVKey -> PlayerCtrlGroup.InputVKey -> DoVKey_Move`，不进入 Stage/效果链；手动入口补为按钮的 `OnPointerDown/OnPointerUp/OnEndDrag/onPressing` 委托进入 `SkillComponent.OnPointerDown/OnPointerUp/OnEndDrag/OnPressingAction`；`InterruptSkill` 改为 `ClientBreakActiveSkill/BreakSkillEntity`；`SkillStage.Enter/Tick/GetTimeLineStage` 改为 `OnEnter/OnUpdate/ExitStage/ExecuteFrameEvents`，`GetTimeLineStage` 归属 `SkillInfo/BaseConfigInfo`；手动效果入口改为 `SkillEntityActionPartial.PlayStageEffect/TryPlayEffect`，`StageHandle` 限定为 ServerControl/Bullet 等路径；`SkillBlackBoard` API 改为继承 `BaseBlackBoard` 的 `Set/Get/Clear`。 |
| `L3B_SKILL_PARTIAL_EXT.md` | 已修正 | partial 方法名按代码修正；`InterruptSkill` 改为 `ClientBreakActiveSkill/BreakSkillEntity`；ActionPartial 改为动画/阶段/效果注册真实方法清单，并补充 `OnActionStageTryPlayEffect -> PlayStageEffect -> TryPlayEffect -> RegisterServerEffect/StageTryPlayServerEffect/FuncOnTryPlayClientEffect` 的已验证链路；Buff/Passive 分布补齐 `OnBuffEndRet`、`OnPassiveSkillEndRet`，`EnterFrameRunnintBuff` 标注为代码实际拼写；UserInput 边界改为 `SkillControllerUserInputPartial` 负责 `_clientInputCache`、阶段未 `ServerCreateStage` 时的 `_waitSendInputCache`、`SendUserInput()` 决策和 `TriggerServerInput/PrePlayUserInput`，协议发送分别落在 `SkillControllerMsgPartial.SendPreUseSkillReq()`、`SkillMsgUtils.SendPreSkillUseInput()`、`sendPreSkillUseInputCancelReq()`；2026-07-16 复核补充 Energy 延迟执行链：`ExecuteEnergyUserInput()` 调 `DelayInvokeExecuteUserInput()`，回调内先 `SkillMsgUtils.SendPreSkillUseInput()` 再 `ExecuteUserInput()`；`SkillEntityUserInputPartial` 负责 `KEY_USER_INPUT` 黑板、客户端/服务器结果合流、`ExecuteSkillUserInput/ExecuteEnergyUserInput` 和蓄力计时；CfgPartial 收紧为配置表读取、阶段/蓄力参数和黑板回填。 |
| `L4_COMBAT_EFFECT.md` | 已修正 | Buff/Bullet/Passive 创建入口、PassiveInfo/PassiveSkillEntity 已修正；Passive 图改为 `PassiveSkillEntity -> PassiveInfo / PassiveStageHandle`；Buff 协议入口补为 `GameManager.HandleRPCMsg -> EntityCtrlMsgBase/CtrlGroup`，`SkillController*Partial` 仅负责 CtrlGroup 之后的运行时处理；Buff 结束说明改为 `OnBuffEndRet` 标记状态、后续 `EnterFrameBuff -> ReleaseTempBuffs` 延迟释放；Bullet 命中不再写成直接回调，改为 `RegisterBulletAction -> StageTryPlayClientEffect/OnFuncStageTryPlayServerEffect` 委托链；补充 Bullet `hasCreate` 防重复创建和 `OnBulletEndRet -> ReleaseBullet` 立即回收边界；L3 依赖补清 `StageHandle` 只承接 ServerControl/Bullet 等阶段效果线，手动 SkillEntity 效果线在 `SkillEntityActionPartial`。 |
| `L5_SUPPORT_SYS.md` | 已修正 | Map 的 IMapLogic 生命周期、ClientNpc 地图配置触发链、Camera 的无参 Update/LateUpdate、Shadow 的事件回调同步、TypeEffect、CusQueue API 均改为代码证实的方法；Map 对外入口收紧为 `GameMap.Load/SetObstacleInfo/EnterFrame`，`LoadMapJson` 标为 private 内部加载，`MapScript` 不写成 `IMapLogic` 驱动者；ClientNpc 补为 `AppMain.InitServices -> ClientNpcManager.Init -> OnSceneMapConfigLoad -> GameMap.sceneJsonData.Npcs/Triggers -> ClientNpc.Create`，帧驱动为 `StarWorldGame.OnEnterFrame -> ClientNpcManager.EnterFrame -> ClientNpc.EnterFrame -> NpcTrigger/State`，AOI 绑定来自 `GameNPCCtrlGroup.OnEnterAOI/OnLeaveAOI`；`SerSnapshotSeqManager` 收紧为现行空 `Init()` + 历史注释逻辑，不再描述 PushFrame/序列化/回放；但 `ServiceSnapshotData` 子类仍作动态缓存：`SerMessageSnapData` 由 NetworkManager 缓存/弹出，`SerRPCOneFrameSnapALLData` 由 GameManager 帧循环弹出，THD/MainPlayer 缓存分发当前入口被注释或早返回；TypeEffect 工厂改为只 `Init`，`OnEnter/OnExit` 由 TypeEffect 持有者触发（`ServerControlStageEntityBase` 覆盖 Buff/Passive/阶段实体，`SkillBullet` 覆盖子弹），`OnShowEnter/OnShowUpdate/OnShowExit` 由 `EntityCtrlBase` 栈事件转到 `ViewVitalNPCNormal` 表现层；工厂映射为 17 类，但 `BUFF_SkillSlotHide` 下游 `Start/StopHiddenSkillSlotEffects` 当前直接 `return` 屏蔽；`GameCameraFeel` 标注当前调用链注释化，技能普通 Camera 链与 CameraShake 链已拆清；补音效边界 `SkillController -> ViewVitalNPCNormal -> SoundManager`；移除“自身不依赖战斗逻辑层”的过宽结论；`LocalDataManager` 仅按已证地图配置读取和音频表懒加载边界描述。 |
| `PLAN_v4.md` | 历史草案 | 顶部已标明不能作为当前代码事实。 |
| `PLAN_v5.md` | 历史草案 | 顶部已标明不能作为当前代码事实，并补充后续 review 纠正项。 |

## 旧报告处理

`reports/agent-effect.md`、`agent-entity-factory.md`、`agent-entity-runtime.md`、`agent-entry.md`、`agent-player.md`、`agent-skill-core.md`、`agent-skill-partial.md`、`agent-support.md` 均已加顶部历史草稿标识。它们保留原始阅读过程，但不再作为当前事实来源。

`reports/*-review.md` 保留原始审查证据和当时的 Required Fixes；截至 2026-07-15，`agent-entry-control-review.md`、`agent-skill-review.md`、`agent-effect-review.md`、`agent-entity-support-plan-review.md` 均已追加 Follow-up，说明对应修正已同步到根文档或摘要文档。

## 仍需谨慎

- 文档中保留的“未证实/未确认”均表示当前核验没有足够代码证据，不应被当作事实。
- `PLAN_v4.md` / `PLAN_v5.md` 是计划文档，内部历史问题不会逐句改写；顶部状态说明是边界。
