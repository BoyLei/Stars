# Star 模块系统组件图

```mermaid
flowchart TB
    subgraph ENTRY["入口"]
        APP["AppMain<br/>InitServices() + InitBusiness()"]
    end

    subgraph CORE["核心管理器"]
        MM["ModuleManager<br/>(ServiceModule 单例)<br/><br/>m_mapModules: Dict<br/>m_mapCacheMessage: Dict<br/>bModuleStack: Stack<br/><br/>CreateModule / GetModule<br/>ReleaseModule / SendMessage<br/>ShowModule / HideModule<br/>Event / Init"]
    end

    subgraph BASE["模块基类"]
        MOD["Module (abstract)<br/>└─ IRegistrable"]
        SVM["ServiceModule&lt;T&gt;<br/>泛型单例 Service"]
        BM["BusinessModule<br/>业务模块基类"]
        LM["LuaModule : BusinessModule<br/>C#↔Lua 桥接<br/>luaTable / OnModuleCreate<br/>OnModuleShow / OnModuleMessage"]
    end

    subgraph CS_MOD["C# 业务模块 (ModuleDef.Name &lt; LuaModuleType=5000)"]
        LOGIN["LoginModule"]
        SW["StarWorldModule"]
        TRIG["TriggerModule"]
        IA["InterActionModule"]
        RT["RingTaskModule"]
        WM["WorldMapModule"]
        EC["EctypeModule"]
        BZ["BattleZoneModule"]
        TUT["TutorialModule"]
        SP["ScenePlayModule"]
        IT_CTRL["ItemControllerModule"]
    end

    subgraph LUA_MOD["Lua 业务模块 (ModuleDef.Name &gt; 5000)"]
        CHAT["ChatModule"]
        TASK["TaskModule"]
        SHOP["ShopModule"]
        GUILD["GuildModule"]
        MAIL["MailModule"]
        BAG["BagModule"]
        EQUIP["EquipModule"]
        PVP["PvpModule"]
        ARENA["ArenaModule"]
        FRIEND["FriendModule"]
        TEAM["TeamModule"]
        RANK["RankModule"]
        DRAW["DrawCardModule"]
        ACT["ActivityModule"]
        WB["WildBossModule"]
        LS["LivingSkillsModule"]
        TALENT["TalentModule"]
        BANK["BankModule"]
        EXCH["ExchangeModule"]
        REWPOP["RewardsPopModule"]
    end

    subgraph SVC_MOD["Service 模块 (ServiceModule 单例)"]
        UIM_SVC["UIManager"]
        LUA_SVC["LuaManager"]
        NET_SVC["NetworkManager"]
        SND_SVC["SoundManager"]
        RES_SVC["ResourceManager"]
        BAT_SVC["BattleManager"]
        CAM_SVC["CameraManager"]
        INP_SVC["InputManager"]
        ATL_SVC["AtlasManager"]
        LNG_SVC["LanguageManager"]
        TM_SVC["TimelineManager"]
    end

    subgraph DEF["模块定义"]
        MDEF["ModuleDef<br/><br/>enum Name {<br/>  None,<br/>  LoginModule, ...<br/>  LuaModuleType = 5000,<br/>  AvgLuaModule,<br/>  TaskModule,<br/>  ChatModule, ...<br/>}<br/><br/>GetModuleDefState()<br/>GetModuleName()"]
    end

    subgraph COMM["通信机制"]
        MSG["SendMessage(target, func, args)<br/>模块间方法调用<br/>支持目标未创建时的消息缓存"]
        EVT["Event(target, type)<br/>模块间事件订阅<br/>支持预监听(PreListen)"]
        GEVT["GlobalEvent<br/>全局静态事件<br/>跨模块/跨层广播"]
    end

    APP -->|"Init(StarProject.Module)"| MM
    APP -->|"CreateModule(枚举)"| MM
    MM -->|"管理"| BM
    MM -->|"使用"| MDEF
    MM -.->|"消息通信"| MSG
    MM -.->|"事件系统"| EVT
    MM -.->|"全局事件"| GEVT

    MOD --> SVM
    MOD --> BM
    BM --> LM

    CS_MOD -->|"继承"| BM
    LUA_MOD -->|"LuaTable 绑定"| LM

    MDEF -.->|"< 5000 = CsModuleDef"| CS_MOD
    MDEF -.->|"> 5000 = LuaModuleDef"| LUA_MOD
    MDEF -.->|"Service 枚举"| SVC_MOD

    style MM fill:#1a5276,color:#fff,stroke:#3498db
    style MDEF fill:#7d6608,color:#fff,stroke:#f1c40f
    style LM fill:#6c3483,color:#fff,stroke:#af7ac5
```

## 架构说明

### 模块创建流程

```
CreateModule("ChatModule")
    │
    ├─ Type.GetType("StarProject.Module.ChatModule")
    │   └─ null → C# 中没有此类
    │
    └─ LuaManager.Instance.GetLuaModule("ChatModule")
        └─ new LuaModule() → BindLuaCall(LuaTable)
            └─ Lua 侧: LuaScripts/LuaModule/ChatModule.lua
```

### C# 与 Lua 分割线

| 类型 | 枚举值 | 判别 | 示例 |
|------|--------|------|------|
| C# 业务模块 | < 5000 | `CsModuleDef` | LoginModule, StarWorldModule |
| Lua 业务模块 | > 5000 | `LuaModuleDef` | ChatModule, TaskModule, ShopModule |

### 通信方式对比

| 方式 | API | 特点 | 适用场景 |
|------|-----|------|----------|
| **SendMessage** | `ModuleManager.SendMessage(target, func, args)` | 反射调用，支持消息缓存 | 模块间方法调用 |
| **Event** | `ModuleManager.Event(target, type).AddListener()` | 支持预监听 | 模块间事件订阅 |
| **GlobalEvent** | `GlobalEvent.onLogin.Invoke()` | 全局广播 | 跨层通知 |
