# Star 项目整体架构 C4 分层图

```mermaid
flowchart TB
    subgraph L3["🔷 StarProjectDef — 定义层"]
        direction LR
        DEF_ENUMS["GameEnums (91KB)<br/>E_EntityType / E_LocalEntityType<br/>UISeatType / ModuleDef.Name"]
        DEF_CONFIG["配置定义<br/>GameConfig / GlobalConfig<br/>AOIAttrDefine (27KB)"]
        DEF_WRAP["数据结构 (350+)<br/>WrapData/ 配置表结构"]
        DEF_COMMON["UIDef / ModuleDef<br/>AssetsToDefine"]
    end

    subgraph L2["🔷 StarGame — 游戏逻辑层"]
        direction LR
        
        subgraph GAME["Game 核心运行时"]
            G_ENTITY["Entity 系统<br/>EntityFactory / ViewFactory<br/>HeroEntity / MonsterEntity<br/>BulletEntity / SummonEntity"]
            G_PLAYER["Player<br/>EntityCtrlBase<br/>PlayerCtrl / MonsterCtrl"]
            G_SKILL["Skill 系统<br/>Timeline 驱动<br/>技能 / Buff / 被动"]
            G_MAP["Map / Camera<br/>StarsCamera<br/>FindPath"]
        end

        subgraph MOD["Module 业务模块 (80+)"]
            M_CS["C# 模块 (&lt;5000)<br/>Login / StarWorld / Trigger<br/>InterAction / RingTask<br/>WorldMap / Ectype / BattleZone"]
            M_LUA["Lua 模块 (&gt;5000)<br/>Chat / Task / Shop / Guild<br/>Mail / Bag / Pvp / Arena<br/>Friend / Team / Rank / DrawCard"]
        end

        subgraph SVC["Service 常驻服务 (30+)"]
            S_RES["ResourceMgr<br/>场景/资源加载"]
            S_LUA["LuaManager<br/>Lua VM 桥接"]
            S_BAT["BattleManager<br/>战斗管理"]
            S_SND["SoundManager<br/>音效管理"]
            S_GM["GMManager<br/>GM调试"]
            S_OTHER["CameraMgr / InputMgr<br/>AtlasMgr / LanguageMgr<br/>TimelineMgr / LocalDataMgr"]
        end

        subgraph UI_GAME["UI 游戏界面"]
            U_LOGIN["Login 界面"]
            U_GUIDE["新手引导"]
            U_MAP["WorldMap 窗口"]
            U_RED["RedPoint 红点"]
            U_DMG["DamageView 伤害"]
            U_SKILL["SkillBtn / SkillIndicator"]
        end
    end

    subgraph L1["🔷 StarFramework (SGF) — 基础框架层"]
        direction LR
        
        subgraph FW_MOD["Module.Framework"]
            MM["ModuleManager<br/>模块生命周期管理<br/>SendMessage / Event"]
            MOD_BASE["Module 抽象基类<br/>BusinessModule<br/>ServiceModule&lt;T&gt;"]
            EVT["EventTable<br/>模块间事件系统"]
        end

        subgraph FW_NET["Network 网络层"]
            NET["NetworkManager<br/>gameSocket / battleSocket"]
            KCP["KCP 可靠UDP"]
            TCP["TCP Socket"]
            RPC["RPCLite<br/>远程过程调用"]
            SS["StateSync<br/>状态同步"]
            MD["MDManager<br/>消息分发"]
        end

        subgraph FW_UI["UI.Framework"]
            UIM["UIManager (ServiceModule)"]
            UI_H["UIPanel → UIPage<br/>UIWindow / UIWidget"]
            UI_Q["UIQueueManager<br/>UI优先级队列"]
            UI_T["Tweening<br/>动画系统"]
        end

        subgraph FW_UTIL["工具层"]
            UNITY["Unity 工具<br/>MonoHelper / Loom<br/>AppPrefs / MonoSingletonEx"]
            STORAGE["Storage<br/>FileUtils / PathUtils"]
            OTHER["Math / Time / Extension<br/>ThdPartExtend"]
        end
    end

    L2 --> L1
    L3 -.->|被两者共享引用| L1
    L3 -.->|被两者共享引用| L2

    style L1 fill:#1a3a5c,color:#fff,stroke:#4a90d9
    style L2 fill:#1a4a3c,color:#fff,stroke:#4ad990
    style L3 fill:#3a3a1a,color:#fff,stroke:#d9d94a
```

## 架构说明

### 三层依赖关系

```
StarProjectDef (定义层)
       ↑ 共享引用
  ┌────┴────┐
  │         │
StarGame   StarFramework
(游戏逻辑)  (基础框架)
  │         ↑
  └────单向依赖──┘
```

### 关键设计原则

| 层次 | 命名空间 | 依赖方向 | 主要内容 |
|------|----------|----------|----------|
| **StarFramework** | `SGF.*` | 只依赖 Unity，不依赖上层 | 模块管理、网络通信、UI框架、工具类 |
| **StarGame** | `StarProject.*` | 依赖 SGF + StarProjectDef | 游戏运行时、业务模块、服务、游戏界面 |
| **StarProjectDef** | `StarProjectDef` | 被两者引用 | 枚举、常量、配置数据结构 |

### 模块系统特点

- **C# / Lua 混合**: 模块由 `ModuleDef.Name` 枚举统一标识，值 < 5000 是 C# 模块，> 5000 是 Lua 模块
- **统一创建**: `ModuleManager.CreateModule()` 自动判断类型，C# 反射实例化或 LuaManager 加载
- **统一通信**: `SendMessage` (方法调用) + `Event` (事件订阅) + `GlobalEvent` (全局广播)

### 文件统计

- **总 C# 文件**: 7,608 个
- **StarFramework**: ~100 个核心框架文件
- **StarGame**: ~7,500 个游戏逻辑文件
- **StarProjectDef**: 散布在 StarGame 中，`WrapData/` 含 350+ 配置结构体
