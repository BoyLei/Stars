<?xml version="1.0" encoding="utf-8"?>
<manifest 
    xmlns:android="http://schemas.android.com/apk/res/android" 
    package="com.unity3d.player"
    xmlns:tools="http://schemas.android.com/tools"
    android:versionName="1.0" android:versionCode="1" android:installLocation="preferExternal">
    <!-- 支持屏幕大小 -->
    <supports-screens android:smallScreens="true" android:normalScreens="true" android:largeScreens="true"
        android:xlargeScreens="true" android:anyDensity="true" />

    <!-- OpenGL ES 版本要求 -->
    <uses-feature android:glEsVersion="0x00020000" />
    <!--  解决 manifest 文件中声明了 Android TV 的特性 -->
<!--    <uses-feature-->
<!--        android:name="android.software.leanback"-->
<!--        android:required="false" />-->
    <!-- 支持压缩纹理 -->
    <supports-gl-texture android:name="GL_OES_compressed_ETC1_RGB8_texture" />

    <application 
        android:theme="@style/UnityThemeSelector"
        android:label="@string/app_name"  
        android:isGame="true" 
        android:allowBackup="true" 
        android:exported="true"
        android:name="com.gs.sdk.GSSDKApplication"
		android:usesCleartextTraffic="true"
       >
       <!-- com.unity3d.player.UnityPlayerActivity -->
        <activity android:name="com.gssdk.unity.GSMainActivity" android:label="@string/app_name" android:exported="true"
            android:screenOrientation="fullSensor" android:launchMode="singleTask"
            android:configChanges="mcc|mnc|locale|touchscreen|keyboard|keyboardHidden|navigation|orientation|screenLayout|uiMode|screenSize|smallestScreenSize|fontScale">
            <intent-filter>
                <!-- 表示 Active 是应用的主入口-->
                <action android:name="android.intent.action.MAIN" />
                <!-- 表示 系统可以从智能手机平台启动-->
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- 表示 系统可以从 Android TV 平台启动-->
<!--                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />-->
            </intent-filter>
            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
            <!-- Required banner for Leanback support (上面的 Android TV 需要这个) -->
<!--            <meta-data-->
<!--                android:name="com.google.android.tv.banner"-->
<!--                android:resource="@drawable/banner" />-->
        </activity>

        <!-- Unity在应用启动时请求所有运行时权限，取消下面的注释可以阻止此操作 -->
        <!--
        <meta-data
            android:name="unityplayer.SkipPermissionsDialog"
            android:value="true" />
        -->
    </application>

    <!-- PERMISSIONS -->
    <!-- 通用权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <!-- Android Goodies 测试应用权限 -->
    <!-- 修改屏幕亮度 -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <!-- 在某些设备上打开蓝牙设置 -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <uses-permission android:name="android.permission.VIBRATE" />



    <!-- 获取GPS坐标 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    


    <!-- 测试运行时权限 -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <!-- 联系人选择器 -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- 录音功能 -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- WiFi 管理 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- GPS 坐标 -->
    <!-- 仅在应用面向 Android 5.0（API 级别 21）或更高版本时需要 -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <!-- ANDROID GOODIES TEST APP PERMISSIONS -->

    <!-- 触摸屏幕 -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen.multitouch.distinct" android:required="false" />

</manifest>