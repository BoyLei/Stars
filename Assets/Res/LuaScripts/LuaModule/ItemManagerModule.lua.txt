--[[
*文件名   :   ItemManagerModule
*内  容   :
*说  明   :
*创建日期 :   "2022/10/27 16:26:40"
*创建人   :   "huqinwen"
*版权所有 :   游卡网络科技技术有限公司
]] ItemManagerModule = class("ItemManagerModule", BaseLuaModule)

-- LuaModule 消息模块
ItemManagerModule.ModuleMessages = {}

ItemManagerModule.ItemList = {}

-- LuaModule 创建
function ItemManagerModule:OnModuleCreate(arg)
    LuaModuleDispatcher:Register(ModuleDef.ItemManagerModule, self)

    -- 刷新背包空间
    -- CS.SGF.Network.NetworkManager.Instance:OnMessageEnum(CS.MsgIDEnum.ItemSpaceSyncListID, function (data)
    -- 	self:OnItemSpaceListRefesh(data)
    -- end, nil);

    -- 刷新道具改变(暂时用不到)
    -- CS.SGF.Network.NetworkManager.Instance:OnMessageEnum(CS.MsgIDEnum.ItemPropSyncListID, function(data)
    --     self:OnItemProbListRefesh(data)
    -- end, nil);

    -- 金币的改变
    CS.SGF.Network.NetworkManager.Instance:OnMessageEnum(CS.MsgIDEnum.PropSyncListID, function(data)
        self:OnProbListRefesh(data)
    end, nil);

end

function ItemManagerModule:OnItemSpaceListRefesh(data)

    print("OnItemSpaceListRefesh");

    local pb = data.data;
    local itemSpaceID = pb.ItemSpaceID;
    print("OnItemSpaceListRefesh" .. pb.ItemSpaceID)
    -- print("OnItemSpaceListRefesh"..data.ItemInfo)
end

function ItemManagerModule:OnItemProbListRefesh(data)

    print("OnItemProbListRefesh");

end

function ItemManagerModule:OnProbListRefesh(data)

    print("OnProbListRefesh");

end

-- LuaModule 消息处理
function ItemManagerModule:OnModuleMessage(msg, ...)
    self.ModuleMessages[msg](...);
end

-- LuaModule显示
function ItemManagerModule:OnModuleShow(arg)

end

-- LuaModule 释放
function ItemManagerModule:OnModuleRelease()
    LuaModuleDispatcher:UnRegister(ModuleDef.ItemManagerModule)
end

return ItemManagerModule

