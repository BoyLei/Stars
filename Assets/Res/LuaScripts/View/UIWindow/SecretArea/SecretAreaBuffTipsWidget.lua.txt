---------------------------------------------------------------------
-- Stars (C) CompanyName, All Rights Reserved
-- Created by: AuthorName
-- Date: 2023-06-06 14:19:35
---------------------------------------------------------------------

-- To edit this template in: Data/Config/Template.lua
-- To disable this template, check off menuitem: Options-Enable Template File

---@class SecretAreaBuffTipsWidget

SecretAreaBuffTipsWidget = class("SecretAreaBuffTipsWidget", UIPanel);

SecretAreaBuffTipsWidget.IsOpen = false;

SecretAreaBuffTipsWidget.buffIconPath = "";
SecretAreaBuffTipsWidget.AtlasPath = "UI/Icons/Atlas/IconBuff";

function SecretAreaBuffTipsWidget:Awake()
	self:Init();
	self.onCloseDestroy(true)
end

function SecretAreaBuffTipsWidget:Init()
	self.transform = self.prefab.transform;
	self.gameObject = self.transform.gameObject;

	self.BuffIcon = self.transform:Find("Center/GngEffectItem/Mask/Bg/BuffIcon"):GetComponent(typeof(Image));
	self.BuffName = self.transform:Find("Center/GngEffectItem/BuffName"):GetComponent(typeof(Text));
	self.BuffLevel = self.transform:Find("Center/GngEffectItem/BuffLevel"):GetComponent(typeof(Text));
	self.BuffDes = self.transform:Find("Center/GngEffectItem/BuffDes"):GetComponent(typeof(Text));
	--self.BuffAddition = self.transform:Find("Center/BuffAddition"):GetComponent(typeof(Text));
	-- 确认【J按钮】
	self.JBtnSure = self.transform:Find("Center/JBtnSure"):GetComponent(typeof(JButton));
	self.JBtnSureFun = function()
		self:OnClickSureJBtn();
	end
	self.JBtnSure.OnClick = self.JBtnSureFun;
end

function SecretAreaBuffTipsWidget:Start()

end

function SecretAreaBuffTipsWidget:OnEnable()

end

function SecretAreaBuffTipsWidget:OnDisable()

end

function SecretAreaBuffTipsWidget:OnDestroy()

end

function SecretAreaBuffTipsWidget:OnOpen(arg)
	self.IsOpen = true;
	--self:InitData(arg);
end

function SecretAreaBuffTipsWidget:OnClose(arg)
	self:CloseWindow();
end

-- 初始化信息
function SecretAreaBuffTipsWidget:InitData(arg)
	self:Rest();

	self.SkillBuff = arg.SkillBuff; -- 这是个被动实体
	self.State = arg.State;

	self.BuffName.text = self.SkillBuff:GetBuffName();
	self.BuffDes.text = self.SkillBuff:GetBuffDecs();
	--local level = self.SkillBuff:GetBuffLevel();
	--self.BuffLevel.text = "Lv." .. level;

	local isShowIcon = (self.State & E_BuffUIShowDetailsEnum.ShowIcon) == E_BuffUIShowDetailsEnum.ShowIcon;
	if isShowIcon then
		-- BUFF图片
		-- 拿取配置表里面的icon路径
		--local buffIconPath = self.BuffEntity:GetBuffIcon2Path();
		--if buffIconPath ~= "" and buffIconPath ~= nil then
		--if self.buffIconPath ~= buffIconPath then
		--local res = SetImage(self.BuffIcon,self.AtlasPath,buffIconPath,self.buffIconPath,true);
		--if res then
		--self.buffIconPath = buffIconPath;
		--end
		--end
		--end
		self.BuffIcon.gameObject:SetActive(true);
	else
		self.BuffIcon.gameObject:SetActive(false);
	end
end

-- 重置基础信息
function SecretAreaBuffTipsWidget:Rest()
	self.SkillBuff = nil;
	self.State = 0;
end

----------------------------------------------------- 按钮点击事件 【STAR】 ------------------------------------------

function SecretAreaBuffTipsWidget:OnClickSureJBtn()
	--self.Close();
    UIManager.Instance:CloseWidget(self.transform.name, nil, false)
end

----------------------------------------------------- 按钮点击事件 【END】 ------------------------------------------


-- 关闭界面
function SecretAreaBuffTipsWidget:CloseWindow()
	self.IsOpen = false;
	self:Rest();
end

return SecretAreaBuffTipsWidget;
